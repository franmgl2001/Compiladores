int main(int a, int b) {
    int sum;
    int i;
    int x [4];
    a = 5;
    b = 3;
    sum = 0;
    // Compute sum = a + b
    sum = a + b;

    // Print result
    output(sum);

    // Loop from i = 0 to i < 5
    i = 0;
    while (i <= 5) {
        output(i, 3+3 *a);
    }
    print();
    i= 5+5;
    return 0;
}

hola = 9;